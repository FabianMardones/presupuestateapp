import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.web.presupuestateapp',
  appName: 'PresupuestateApp',
  webDir: 'www'
};

export default config;
