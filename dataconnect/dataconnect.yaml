specVersion: "v1"
serviceId: "presupuestateapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "presupuestateapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
